# Restart-Wallpaper-Installer
Automatically Installs choosen wallpaper on restart. Use this for prank, entartainment propose ONLY
Use W.bat's raw | Example of using: curl -L ***RAW LINK TO W.bat*** > w.bat && w.bat
